Kokkos::OpenMP::initialize WARNING: OMP_PROC_BIND environment variable not set
  In general, for best performance with OpenMP 4.0 or better set OMP_PROC_BIND=spread and OMP_PLACES=threads
  For best performance with OpenMP 3.1 set OMP_PROC_BIND=true
  For unit testing set OMP_PROC_BIND=false

terminate called after throwing an instance of 'std::runtime_error'
  what():  Error: Kokkos::deep_copy with no available copy mechanism: from source view ("A_host") to destination view ("A").
There is no common execution space that can access both source's space
(Host) and destination's space (SYCLDeviceUSM), so source and destination
must be contiguous and have the same layout.

